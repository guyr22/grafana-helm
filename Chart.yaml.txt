# Chart.yaml
# This file defines the chart's metadata.
# The 'dependencies' section has been removed because we are now
# embedding the PostgreSQL templates directly.

apiVersion: v2
name: grafana-postgres
description: A Helm chart for deploying Grafana and a dedicated PostgreSQL database for it.
type: application

# This is the chart version.
version: 0.1.0

# This is the version of the application being deployed.
appVersion: "10.4.2"
